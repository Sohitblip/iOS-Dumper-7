name: Build Dumper dylib

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Xcode
      run: sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

    - name: Compile dylib using xcodebuild
      run: |
        xcodebuild -project Dumper.xcodeproj -scheme Dumper -configuration Release -sdk iphoneos CONFIGURATION_BUILD_DIR=./build

    - name: Upload compiled .dylib Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Dumper-compiled-dylib
        path: ./build/*.dylib
